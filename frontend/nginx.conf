# Serve static files
location / {
    root /usr/share/nginx/html;
    try_files $uri $uri/ /index.html;
}

# Proxy API requests to backend
location /api/ {
    proxy_pass http://backend:3000; # ECS resolves backend
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
}